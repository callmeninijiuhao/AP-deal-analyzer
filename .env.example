# AP Gap Analyzer Proxy Environment Config
# Copy this file to .env to customize settings

# Port where the local CORS proxy runs (server side)
PROXY_PORT=3002

# Port the browser uses to talk to the local CORS proxy (must match PROXY_PORT)
VITE_PROXY_PORT=3002
